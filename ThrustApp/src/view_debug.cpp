#include "view_debug.h"

DebugView::DebugView(QWidget* parent) : QWidget(parent)
{
    //labCommand = new QLabel("command", this);
    //labData = new QLabel("data", this);

    //txtView = new QTextEdit(mainWidget);
    //layout->addWidget( txtView);

}
